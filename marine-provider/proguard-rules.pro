# The provider contains no reflection-based serialization. Keep service classes
# because the Android framework instantiates them from the manifest.
-keep class com.tideglass.surf.provider.complications.** { *; }

